- name: Install the chrome .deb package
  tags: chrome, ubuntu
  apt:
    deb: https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
    state: present
    update_cache: yes
    force_apt_get: yes
